<?php
session_start();

if (isset($_SESSION['cart']) && !empty($_SESSION['cart'])) {
    // Получаем содержимое корзины из сессии
    $cartItems = $_SESSION['cart'];

    // Здесь можно выполнить запрос к базе данных для получения информации о товарах и их цене
    // Например: SELECT SUM(price) AS total FROM products WHERE id IN ($cartItems)

    // Предположим, что мы имеем переменную $total, содержащую общую стоимость товаров
    $total = 0;

    // Выводим общую стоимость товаров
    echo "<p>Общая стоимость: $total руб.</p>";
} else {
    // Выводим сообщение, если корзина пуста
    echo "<p>Корзина пуста</p>";
}
?>
